import { PromptModal, TabModal } from '.'

describe('PromptModal', () => {
  it('is truthy', () => {
    expect(PromptModal).toBeTruthy()
  })
})

describe('TabModal', () => {
  it('is truthy', () => {
    expect(TabModal).toBeTruthy()
  })
})
